import CustomSelect from '../../../components/common/CustomSelect/CustomSelect';
function QuestionMatrix({ worksheet, view, onViewChange, onSelect, selection }) {
    return <section className="diagnosis-card matrix-card"><div className="diagnosis-card__heading"><div><span>QUESTION MATRIX</span><h2>문항별 성취</h2></div><CustomSelect label="보기 방식" value={view} onChange={onViewChange} width={124} options={[{ value: 'score', label: '보기: 득점' }, { value: 'time', label: '보기: 시간' }]} /></div><div className="matrix-legend">{view === 'score' ? <><span>■ 만점</span><span>▨ 부분</span><span>□ 0점</span><span>◐ 힌트</span><span>· 미제출</span></> : <><span>연함 = 빠름</span><span>진함 = 느림</span></>}</div><div className="matrix-table__wrap"><table className="matrix-table matrix-table--questions"><thead><tr><th>학생</th>{worksheet.questions.map((question) => <th key={question.no}><button type="button" onClick={() => onSelect({ type: 'question', questionNo: question.no })}>{question.no}번</button></th>)}</tr></thead><tbody>{worksheet.students.map((student) => <tr key={student.id}><th>{student.name}</th>{student.responses.map((response) => { const pending = response.gradedBy === null; const ratio = response.score / response.maxScore; const symbol = pending ? '·' : response.hintUsed && ratio === 1 ? '◐' : ratio === 1 ? '■' : ratio > 0 ? '▨' : '□'; return <td key={response.no}><button type="button" className={`question-cell${selection?.type === 'question' && selection.questionNo === response.no ? ' question-cell--selected' : ''}`} style={view === 'time' ? { '--time-opacity': Math.min(.82, .14 + response.seconds / 360) } : undefined} onClick={() => onSelect({ type: 'question', questionNo: response.no })}>{view === 'score' ? symbol : `${Math.floor(response.seconds / 60)}:${String(response.seconds % 60).padStart(2, '0')}`}</button></td>; })}</tr>)}</tbody></table></div></section>;
}
export default QuestionMatrix;
