# 프로젝트 디자인 시스템

이 문서는 교사가 수업과 학생 데이터를 빠르게 읽고 다음 행동을 결정하도록 돕는 제품 UI의 기준이다. 화면은 홍보용 랜딩 페이지보다, 자주 사용해도 피로하지 않은 업무 도구처럼 보여야 한다.

## 1. 디자인 원칙

### 판단을 먼저 보여준다

- 페이지를 열었을 때 `현재 무엇을 보고 있는지`, `무엇이 중요한지`, `다음에 무엇을 할 수 있는지`가 첫 화면 안에서 드러나야 한다.
- 제목 아래에는 환영 문구보다 대상, 기간, 갱신 시각처럼 판단에 필요한 맥락을 둔다.
- 한 섹션은 한 가지 질문에 답한다. 같은 수치나 결론을 요약 카드, 차트, 설명에서 같은 강도로 반복하지 않는다.
- 정확한 값의 조회는 표, 비교는 막대나 분포, 결론은 짧은 문장으로 표현한다.

### 업무 화면다운 밀도를 유지한다

- 모든 내용을 큰 카드로 띄우지 않는다. 페이지 배경, 구분선, 간격만으로 충분히 나뉘는 영역은 별도 카드로 만들지 않는다.
- 여백은 소속 관계를 표현한다. 제목과 설명은 가깝게, 서로 다른 섹션은 더 멀게 둔다.
- 반복 업무에 필요한 정보는 접거나 숨기지 않고 한 번에 훑을 수 있는 밀도로 제공한다.
- 장식 때문에 정보가 아래로 밀리거나 한 화면의 데이터 양이 지나치게 줄지 않게 한다.

### 차분하고 구체적인 문구를 쓴다

- “한눈에”, “스마트하게”, “완벽한”, “~해요”처럼 의미가 약한 홍보성 문구를 습관적으로 쓰지 않는다.
- 제목은 기능이나 데이터의 이름으로, 설명은 기준·단위·사용 방법으로 작성한다.
- 영문 대문자 키커, 의미 없는 순번, 장식용 라벨을 사용하지 않는다.
- 사용자에게 필요한 경우에만 친근한 어조를 사용하고, 데이터 화면에서는 짧은 서술형 문장을 우선한다.

## 2. 정보 구조와 레이아웃

- 페이지 기본 흐름은 `페이지 맥락 → 조회 조건 → 핵심 요약 → 분석 근거 → 상세 데이터/행동` 순서로 구성한다.
- 페이지 제목, 필터, 본문, 표의 좌우 기준선을 하나의 컨테이너에 맞춘다.
- 기본 정렬은 왼쪽 정렬이다. 중앙 정렬은 빈 상태나 완료 상태처럼 단일 메시지에만 사용한다.
- 같은 행에는 실제 중요도와 역할이 비슷한 요소만 배치한다. 의미가 다른 항목을 같은 크기의 카드로 강제하지 않는다.
- 주요 분석은 더 넓게, 보조 현황과 행동은 더 좁게 배치할 수 있다. 화면을 균등 분할하는 것을 기본값으로 삼지 않는다.
- 데스크톱 본문 최대 너비는 화면 성격에 맞춰 `1280~1440px` 안에서 정하고, 좌우 여백은 최소 `32px`을 확보한다.
- 좁은 화면에서는 글자를 줄이지 않고 열을 쌓는다. 긴 표만 해당 영역 안에서 가로 스크롤을 허용한다.

## 3. 색상

### 기본 팔레트

- 페이지 배경: `#F5F6F8`
- 기본 표면: `#FFFFFF`
- 강한 텍스트: `#172033`
- 본문 텍스트: `#465166`
- 보조 텍스트: `#747F91`
- 기본 경계선: `#DDE2E8`
- 약한 구분선: `#EBEEF2`
- 주요 상호작용: 기존 서비스 파란색 계열

### 사용 규칙

- 파란색은 링크, 선택, 주요 버튼, 포커스 등 상호작용에 우선 사용한다.
- 빨강·주황·초록은 위험, 주의, 완료처럼 상태 의미가 있을 때만 사용하고 문구나 아이콘을 함께 제공한다.
- 한 화면에서 상태색을 제외한 강조색은 하나를 기본으로 한다.
- 데이터 계열이 아닌 카드마다 서로 다른 색을 부여하지 않는다.
- 그라데이션, 반투명 색면, 글로우, 장식용 배경 무늬는 사용하지 않는다.

## 4. 타이포그래피

- `src/index.css`의 Pretendard 구성을 유지한다.
- 페이지 제목은 기본 `24~28px / 700`, 섹션 제목은 `17~20px / 700`, 본문은 `13~15px / 400~500` 범위에서 사용한다.
- 표, 탭, 버튼, 일반 본문은 `12px` 이상, 상태 배지와 보조 정보는 `11px` 이상을 유지한다.
- 숫자는 단위와 가까이 두고 동일한 종류의 값은 같은 크기와 정밀도로 표시한다.
- 굵기 단계는 한 화면에서 가능한 `400`, `600`, `700` 세 단계 안으로 제한한다.
- 자간을 넓힌 영문 라벨, 그라데이션 글자, 과도하게 큰 숫자는 사용하지 않는다.

## 5. 표면과 형태

- 기본 섹션 표면은 흰색, `1px` 경계선, `8~12px` 반경을 사용한다.
- 그림자는 드롭다운, 모달, 고정 선택 바처럼 실제로 겹쳐 있는 요소에만 사용한다.
- 일반 콘텐츠 카드는 hover 시 위로 움직이거나 그림자가 커지지 않는다. 클릭 가능하면 배경색·테두리색·글자색 변화로 피드백한다.
- pill 형태는 상태, 필터 값, 짧은 분류처럼 캡슐 모양이 의미를 가질 때만 사용한다.
- 카드 안에 카드를 중첩하지 않는다. 선택된 하위 정보는 옅은 배경이나 한쪽 경계선으로 구분한다.
- 모서리 반경은 같은 역할끼리 통일하고, 원형 장식이나 색상 타일에 아이콘을 습관적으로 넣지 않는다.

## 6. 컴포넌트 기준

### 요약 지표

- 요약 지표는 실제로 동등한 값일 때만 같은 크기의 묶음으로 배치한다.
- `라벨 → 값 → 비교 또는 기준`의 세 단계만 기본으로 두며 장식용 아이콘과 순번을 넣지 않는다.
- 교사용 대시보드 상단 요약은 Ant Design의 `Row`, `Col`, `Card`, `Statistic`과 페이지 전용 SCSS를 사용한다.
- 주의가 필요한 값은 카드 전체를 색칠하지 않고 상태 문구나 얇은 강조선으로 표시한다.

### 버튼과 탭

- 한 영역의 주요 버튼은 하나만 둔다. 나머지는 보조 버튼이나 텍스트 링크로 낮춘다.
- 버튼 문구는 결과가 예상되는 동사형으로 작성한다.
- 클릭 가능한 버튼은 비활성 상태를 제외하고 명확한 hover와 `:focus-visible` 상태를 제공한다.
- 탭은 같은 데이터의 보기 기준을 바꿀 때만 사용한다. 탭 자체를 카드처럼 과도하게 띄우지 않는다.

### 표

- 텍스트 열은 왼쪽, 숫자 열과 헤더는 오른쪽 정렬한다. 선택·상태 열만 필요에 따라 중앙 정렬한다.
- 헤더는 옅은 단색 배경이나 구분선으로 구분하고, 둥근 셀 묶음처럼 꾸미지 않는다.
- 행 hover는 클릭 가능한 행에만 적용하며 키보드 포커스와 동일한 탐색 결과를 제공한다.
- 상태는 색만으로 전달하지 않고 짧은 텍스트를 함께 표시한다.

### 차트

- 관계나 차이를 텍스트보다 빠르게 이해할 수 있을 때만 차트를 사용한다.
- 축, 단위, 기준선을 명시하고 0~100% 같은 비율 축은 특별한 이유가 없으면 전체 범위를 유지한다.
- 격자와 배경 영역은 데이터보다 약하게 표현한다. 색상 수는 해석에 필요한 최소로 제한한다.
- 정확한 값은 툴팁, 직접 라벨, 표 등 다른 방식으로도 확인할 수 있어야 한다.
- 교사용 대시보드의 학생 성취 분포는 Recharts `ScatterChart`를 사용하고 X축은 단원 학습 진행률, Y축은 정답률로 유지한다.

## 7. 아이콘과 이미지

- 아이콘은 텍스트만으로 행동을 알아보기 어려울 때 사용한다. 모든 제목이나 지표 앞에 붙이지 않는다.
- 새 아이콘 라이브러리나 이미지를 추가하기 전에 Bootstrap Icons의 `bi` 클래스를 우선 사용한다.
- 이모지는 콘텐츠 자체가 아닌 이상 UI 장식으로 사용하지 않는다.
- 실제 사용자나 학습 자료 이미지가 없는 데이터 화면에 추상 일러스트를 채움 요소로 추가하지 않는다.

## 8. 접근성과 반응형

- 하나의 명확한 `h1`과 순서가 맞는 제목 구조를 사용하고 DOM 순서를 읽기 순서와 일치시킨다.
- 색상 대비는 WCAG AA를 충족하고, 색 외의 상태 단서를 제공한다.
- 아이콘 버튼, 검색, 드롭다운, 체크박스 등에는 명확한 접근 가능한 이름을 제공한다.
- 키보드 포커스 표시를 제거하지 않으며 기존의 Enter, Space, Escape 동작을 유지한다.
- 자동 재생, 반복 펄스, 스크롤 등장, 바운스, 장식용 hover 이동을 사용하지 않는다.
- `prefers-reduced-motion` 환경에서도 정보와 기능이 완전해야 한다.

## 9. 구현 전 체크리스트

1. 첫 화면에서 대상, 기준 시점, 중요한 상태가 보이는가?
2. 각 섹션이 서로 다른 질문에 답하는가?
3. 색과 효과를 제거해도 크기, 굵기, 위치, 간격으로 위계가 보이는가?
4. 삭제해도 이해에 영향이 없는 카드, 아이콘, 라벨, 문구가 남아 있지 않은가?
5. 숫자의 단위·기간·대상·비교 기준이 가까이 표시되는가?
6. 차트의 축과 기준, 표의 정렬과 상태 표현이 정확한가?
7. 좁은 화면에서도 글자 크기를 유지하며 자연스럽게 재배치되는가?
8. hover, focus, 키보드 조작, 접근 가능한 이름이 제공되는가?

## 10. 피해야 할 패턴

- 파스텔 그라데이션 카드와 카드별 무지개색 강조
- 글로우, 블러, 유리 효과, 떠오르는 hover 모션
- 모든 섹션을 큰 둥근 카드로 반복하는 대시보드
- 영문 대문자 키커, 의미 없는 `01`, `02` 순번
- 모든 제목과 지표에 색상 타일 아이콘 배치
- 설명 없이 큰 숫자만 나열하기
- 같은 결론을 요약 카드, 차트, 콜아웃에서 반복하기
- 상태가 아닌 일반 메타데이터를 모두 pill로 표현하기
- 작은 회색 글씨로 밀도를 억지로 맞추기
- “한눈에”, “스마트”, “맞춤형” 같은 추상 문구로 기능을 설명하기
