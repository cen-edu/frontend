# 프로젝트 디자인 시스템

이 문서는 새 화면을 만들거나 기존 화면을 크게 개편할 때 적용하는 공통 디자인 기준이다. 목표는 교육 데이터를 빠르게 이해하고 다음 행동을 결정할 수 있는, 차분하고 명확한 교사용 서비스 UI다.

## 적용 범위와 우선순위

- 새 페이지, 새 공통 컴포넌트, 레이아웃을 포함한 큰 UI 개편에는 이 문서를 기본값으로 적용한다.
- 작은 수정에서는 기존 화면의 시각 언어를 유지한다. 요청 범위 밖의 화면을 디자인 시스템에 맞춘다는 이유로 함께 재설계하지 않는다.
- 기준이 충돌하면 다음 순서를 따른다.
  1. 데이터, 문구, 단위, 기간, 상태, 접근성 및 사용자 요구사항의 정확성
  2. 기존 React/JSX, SCSS, BEM, 라우팅 및 공통 컴포넌트 구조
  3. 사용자가 가장 먼저 이해하거나 결정해야 할 내용의 위계
  4. 이 문서의 시각 원칙과 세부 표현
- Vercel 참고 자료의 정보 설계와 절제 원칙은 활용하되, Vercel 로고, Geist 글꼴, `vbg-*` 클래스 및 Vercel 전용 브랜드 자산은 이 프로젝트에 도입하지 않는다.

## 핵심 방향

- 화면은 장식보다 과업을 먼저 설명해야 한다. 사용자가 누구를, 무엇을, 어떤 상태로 보고 있으며 다음에 무엇을 할 수 있는지가 첫 화면에서 드러나야 한다.
- 중요한 결론이나 행동은 하나의 주된 시각적 초점으로 만든다. 모든 카드, 수치, 버튼을 같은 강도로 강조하지 않는다.
- 색, 효과, 아이콘, 카드 수가 아니라 크기, 굵기, 위치, 간격, 정렬로 위계를 먼저 만든다.
- 빠르게 훑는 경로와 자세히 확인하는 경로를 함께 제공한다. 제목, 핵심 수치, 상태, 주요 행동만 읽어도 요지가 보여야 하며 표, 설명, 근거에서는 정확한 값을 확인할 수 있어야 한다.
- 한 주장이나 수치의 대표 위치는 하나로 정한다. 요약 카드, 차트, 본문, 결론에서 같은 내용을 같은 비중으로 반복하지 않는다.

## 화면 구성

### 정보 구조

- 화면을 만들기 전에 사용자의 핵심 질문, 가장 중요한 답, 그 답의 근거, 해석을 바꾸는 예외나 제한을 정한다.
- 섹션마다 새로운 질문 하나에 답한다. 역할이 겹치는 섹션은 합치고 의례적인 소개 문구는 줄인다.
- 첫 화면에는 큰 제목만 두지 말고 핵심 상태, 비교, 근거 또는 주요 도구를 함께 보여준다.
- 정밀 조회는 표, 하나의 결론은 짧은 문장, 관계나 추세는 차트로 표현한다. 값이 있다는 이유만으로 차트를 추가하지 않는다.
- 화면의 끝은 결과, 다음 행동, 해석 또는 남은 질문으로 마무리한다.

### 그리드와 정렬

- 페이지 제목, 필터, 본문, 표, 주요 행동은 공통 컨테이너의 좌우 기준선에 맞춘다.
- 동일한 역할의 요소는 제목 기준선, 값의 시작점, 내부 여백, 버튼 위치를 맞춘다.
- 기본 본문과 정보는 좌측 정렬한다. 제목, 본문, 버튼을 습관적으로 모두 중앙 정렬하지 않는다.
- 중앙 정렬은 빈 상태, 완료 상태처럼 짧고 단일한 메시지나 명확한 한 개의 초점에만 사용한다.
- 의미가 다른 항목을 같은 크기의 카드로 강제하지 않는다. 중요한 항목은 더 넓거나 먼저 배치하고 보조 항목은 작거나 조용하게 배치한다.
- 반복 항목은 실제로 동등한 항목일 때만 같은 형태로 반복한다.
- 의도 없는 빈 공간, 마지막 행의 고립된 카드, 한쪽만 비는 분할 레이아웃이 생기면 열 수나 배치를 다시 정한다.

### 여백과 밀도

- 여백은 모든 곳에 같은 값으로 넣지 않고 관계를 표현한다. 제목과 설명은 가깝게, 하나의 정보 그룹과 다음 섹션은 더 멀게 둔다.
- 컴포넌트 내부 간격보다 컴포넌트 사이 간격을 크게 해 소속 관계가 보이게 한다.
- 억지로 화면을 채우거나 한 화면에 모두 넣기 위해 글자를 줄이지 않는다. 내용을 줄이거나 구획을 나누고, 좁은 화면에서는 재배치한다.
- 데이터 밀도가 높은 화면에서도 탭, 표, 버튼, 일반 본문은 12px 이상, 상태 배지와 보조 정보는 11px 이상을 유지한다.

## 시각 언어

### 색상

- 기본은 흰색과 옅은 회색 표면, 짙은 남청색 계열 텍스트, 기존 서비스의 파란색 상호작용 색상을 사용한다.
- 파란색은 주요 행동, 선택, 링크, 포커스처럼 기능적 의미가 있을 때 우선 사용한다.
- 성공, 경고, 오류, 진행 상태 색상은 의미가 있을 때만 사용하고 텍스트나 아이콘 등 비색상 단서를 함께 제공한다.
- 한 영역에서 여러 강조색을 경쟁시키지 않는다. 주 강조색 하나와 필요한 상태색만 남긴다.
- 단순히 보기 좋게 만들기 위한 보라색 그라데이션, 그라데이션 글자, 무지개색, 장식용 색면은 사용하지 않는다.
- 새 색상을 임의로 추가하기 전에 같은 역할을 하는 기존 공통 컴포넌트와 인접 화면의 색상을 재사용한다.

### 타이포그래피

- `src/index.css`의 Pretendard 글꼴 구성을 유지한다. 코드, 경로, 원시 식별자 외에는 별도의 모노스페이스 글꼴을 사용하지 않는다.
- 제목은 단색, 문장형 표현과 크기·굵기로 강조한다. 제목 글자에 그라데이션이나 장식 효과를 적용하지 않는다.
- 페이지마다 임의의 글자 크기 체계를 새로 만들지 않는다. 같은 역할의 제목, 수치, 본문, 보조 문구는 같은 크기와 굵기를 사용한다.
- 큰 제목에서 한 단어만 외롭게 다음 줄로 떨어지지 않도록 문구, 너비, 줄바꿈을 조정한다.
- 본문은 지나치게 긴 한 줄을 피하고, 밀도를 맞추기 위해 작은 회색 글씨로 축소하지 않는다.
- 영문 대문자와 넓은 자간의 장식용 라벨을 반복하지 않는다. 라벨은 분류나 순서 확인에 꼭 필요할 때만 사용한다.

### 표면, 경계와 효과

- 카드와 테두리는 실제 그룹, 선택, 상호작용, 경고를 구분할 때만 사용한다. 간격과 정렬만으로 구분되는 내용에는 박스를 추가하지 않는다.
- 모든 섹션과 수치를 카드로 감싸거나 카드를 중첩하지 않는다.
- 그림자, 블러, 유리 효과, 광택, 빛남, 장식용 배경 그리드와 질감은 사용하지 않는다.
- 그림자는 모달, 드롭다운, 고정 선택 바처럼 겹침과 높이를 전달해야 하는 요소에 제한하고 강도를 낮게 유지한다.
- 모서리 반경은 같은 유형에서 일관되게 사용한다. 일반 정보를 무조건 pill 형태로 만들지 않는다.
- 장식이 부족해 보일 때 효과를 더하기보다 핵심 요소의 크기, 여백, 배치, 대비를 먼저 조정한다.

### 카드

- 같은 크기의 카드가 줄줄이 반복되는 구성을 기본값으로 삼지 않는다.
- 카드가 필요하면 한 카드에 하나의 명확한 역할을 부여한다. 제목, 핵심 값 또는 내용, 필요한 보조 설명만 둔다.
- 중요 카드와 보조 카드의 차이는 실제 정보 중요도에 근거해야 한다.
- 요약 수치가 서로 진짜 동등한 경우에만 동일 카드 묶음을 사용한다. 교사용 대시보드 요약 카드는 `AGENTS.md`에 지정된 Ant Design 구성을 따른다.

### 아이콘, 이모지와 라벨

- 아이콘은 의미 구분이나 행동 인식이 텍스트보다 빨라질 때만 사용한다. 모든 항목 앞에 관습적으로 붙이지 않는다.
- 기존 Bootstrap Icons를 우선 사용하고 한 화면에서 스타일과 크기를 통일한다.
- 이모지는 콘텐츠 자체가 아닌 이상 장식용으로 사용하지 않는다.
- 아이콘을 임의의 색상 타일 안에 넣거나 큰 장식 요소로 확대하지 않는다.
- 배지와 라벨은 상태, 분류, 순서를 구분해야 할 때만 쓴다. 평범한 메타데이터를 모두 캡슐 형태로 만들지 않는다.

## 데이터 표현

- 수치 가까이에 단위, 기간, 대상, 기준을 표시한다. 비교 값은 같은 단위와 정밀도를 사용한다.
- 수치 열과 헤더는 우측 정렬하고 텍스트 열과 헤더는 좌측 정렬한다. 단순 열이나 선택 열 외에는 관습적으로 중앙 정렬하지 않는다.
- 표는 정확한 조회가 목적일 때 사용하고 가능한 증거 영역의 너비를 충분히 사용한다. 좁은 카드 안에 큰 표를 억지로 넣지 않는다.
- 그래프는 값의 관계가 글이나 표보다 빨리 이해될 때만 사용한다. 동일 계열은 동일 축과 척도를 사용하며 차이를 과장하는 잘린 축을 피한다.
- 범례보다 데이터 가까이의 직접 라벨을 우선하고, 색상만으로 계열이나 상태를 구분하지 않는다.
- 차트가 핵심 근거라면 충분한 크기와 대비를 제공하고, 정확한 데이터는 접근 가능한 표나 텍스트로도 확인할 수 있게 한다.
- 막대의 길이가 값을 인코딩하지 않는다면 장식용 막대를 쓰지 않고 정렬된 텍스트를 사용한다.

## 상호작용과 모션

- 상호작용은 상태를 바꾸거나 과업을 완료하는 데 필요한 경우에만 추가한다.
- 기본 화면은 애니메이션 없이도 완전해야 한다. 모션은 상태 전환, 연속성, 행동 완료를 설명하는 짧은 피드백에만 사용한다.
- 자동 재생, 반복 펄스, 타이핑 효과, 스크롤 등장 효과, 패럴랙스, 바운스와 장식용 hover 이동은 사용하지 않는다.
- hover는 클릭 가능성을 분명히 알리는 색상·배경·테두리 변화로 제공하고, 키보드 포커스와 `prefers-reduced-motion`을 보존한다.
- 입력값을 임의로 보정하거나 조용히 초기화하지 않는다. 오류는 입력 가까이에서 이유와 해결 방법을 알려준다.

## 반응형과 접근성

- DOM 순서를 읽기 순서로 유지하고 랜드마크, 하나의 명확한 `h1`, 순서가 맞는 제목 구조, 네이티브 컨트롤을 사용한다.
- 색상 대비는 WCAG AA를 충족하고 색만으로 상태를 전달하지 않는다.
- 그리드와 flex 자식에는 필요한 경우 `min-width: 0`을 주고, 페이지 전체의 가로 넘침을 숨겨 문제를 감추지 않는다.
- 좁은 화면에서는 글자를 축소하기보다 열을 쌓고, 비교 구조를 재배치하고, 정밀 조회용 긴 표만 국소 가로 스크롤을 허용한다.
- 아이콘 버튼, 검색, 필터, 드롭다운, 체크박스에는 명확한 접근 가능한 이름을 제공한다.

## 구현 전 점검

1. 이 화면에서 사용자가 가장 먼저 알아야 할 내용 또는 해야 할 행동이 하나로 보이는가?
2. 각 섹션이 서로 다른 질문에 답하며 같은 결론을 반복하지 않는가?
3. 색과 효과를 제거해도 크기, 굵기, 정렬, 여백만으로 위계가 보이는가?
4. 불필요한 카드, 테두리, pill, 라벨, 아이콘, 장식 효과를 더 제거할 수 있는가?
5. 표와 차트의 단위, 기준, 정렬, 척도와 상태 표현이 정확한가?
6. 데스크톱과 좁은 화면 모두에서 재배치되며 읽을 수 있는 글자 크기를 유지하는가?
7. hover, focus, 키보드 조작, 접근 가능한 이름과 비색상 상태 단서가 있는가?

## 피해야 할 대표 패턴 10가지

1. 보라색·파란색 그라데이션을 배경, 버튼, 카드에 반복 사용하기
2. 제목 글자에 그라데이션 넣기
3. glow, glass, blur, shine 등 여러 효과를 동시에 사용하기
4. 같은 크기의 카드만 줄줄이 반복하기
5. 제목, 본문, 버튼까지 모두 중앙 정렬하기
6. 구분이 필요하지 않은 모든 항목에 아이콘 붙이기
7. 이모지와 장식 기호를 반복해 강조하기
8. 위아래 간격의 관계 없이 획일적인 여백 사용하기
9. 분류나 순서 기능이 없는 작은 라벨을 반복하기
10. 한 화면에 모두 넣기 위해 글자를 계속 줄이기
