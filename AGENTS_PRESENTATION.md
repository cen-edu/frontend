# 프로젝트 개발 지침

AI는 아래 프로젝트 구조와 공통 규칙을 기준으로 코드를 작성한다.
페이지마다 새로운 방식을 만들지 않고, 기존 코드와 공통 모듈을 우선 재사용한다.

## 프로젝트 구조

```text
src/
├─ api/          # 공용 HTTP 클라이언트와 도메인별 API 함수
├─ components/   # 여러 화면에서 재사용하는 공통 컴포넌트
├─ config/       # 메뉴와 애플리케이션 설정
├─ mocks/        # 화면 개발용 더미 데이터와 공통 라벨
├─ pages/        # 라우트 단위 페이지와 페이지 전용 컴포넌트
├─ App.jsx       # 라우트 구성
└─ index.css     # 전역 스타일과 디자인 토큰
```

## 기술 스택

- React와 Vite 기반의 JavaScript/JSX 구성을 유지한다.
- 패키지 관리는 `npm`을 사용하고 `package-lock.json`을 유지한다.
- 페이지 이동은 `react-router-dom`을 사용한다.
- 서버 상태 관리는 `@tanstack/react-query`를 사용한다.
- HTTP 통신은 `axios` 기반의 공용 클라이언트를 사용한다.
- 차트는 `recharts`, 공통 UI는 기존 `antd` 컴포넌트를 우선 활용한다.
- 새로운 라이브러리를 추가하기 전에 현재 의존성으로 구현 가능한지 확인한다.

## API 호출 규칙

- 모든 API 요청은 `src/api/httpClient.js`의 Axios 인스턴스를 사용한다.
- 페이지나 컴포넌트에서 Axios 인스턴스를 새로 생성하지 않는다.
- API 함수는 `src/api` 아래에서 도메인별 파일로 분리한다.
- 컴포넌트는 URL이나 응답 변환 규칙을 직접 다루지 않는다.
- API 명세 변경이 필요한 경우 임의로 처리하지 않고 작업을 중단한 뒤 공유한다.
- 클라이언트 환경 변수는 `VITE_` 접두사를 사용하며 민감 정보는 저장하지 않는다.

## React Query 규칙

- 서버 데이터의 조회와 변경은 React Query로 관리한다.
- 전역 기본 설정은 `src/api/queryClient.js`에서만 관리한다.
- Query Key는 기능별로 일관된 구조를 사용한다.
- Query Function의 `signal`을 Axios 요청 옵션에 전달해 불필요한 요청을 취소한다.
- 모달, 탭, 체크박스 등 화면 내부 상태는 React의 로컬 상태로 관리한다.

## 컴포넌트 분리 기준

- 페이지 컴포넌트는 데이터 연결과 화면 구성을 담당한다.
- 반복되는 UI는 공통 컴포넌트로 분리해 중복 구현하지 않는다.
- 페이지에서만 사용하는 UI는 해당 페이지 폴더의 `components`에 둔다.
- 데이터 변환과 집계 로직은 컴포넌트 밖의 Hook, Adapter 또는 Utility로 분리한다.
- 단순히 파일 길이를 줄이기 위한 의미 없는 컴포넌트 분리는 하지 않는다.

## 스타일 규칙

- 컴포넌트 스타일은 같은 폴더의 `SCSS` 파일에 작성한다.
- CSS 클래스 이름은 기존 코드와 동일하게 BEM 형식을 사용한다.
- 전역 색상, 글꼴, 배경 변수는 `src/index.css`의 디자인 토큰을 재사용한다.
- 공통 화면의 시각 언어를 유지하고 요청 범위 밖의 화면은 함께 변경하지 않는다.
- 아이콘은 새로운 라이브러리보다 기존 Bootstrap Icons를 우선 사용한다.

## 작업 원칙

- 기존 폴더 구조와 구현 방식을 먼저 확인한 뒤 작업한다.
- 한 파일에 상태, 데이터 처리, 레이아웃이 과도하게 집중되지 않도록 설계한다.
- 화면 개발용 더미 데이터는 페이지 안에 선언하지 않고 `src/mocks`에서 관리한다.
- 사용자가 요청하지 않은 기능이나 구조 변경은 임의로 추가하지 않는다.
